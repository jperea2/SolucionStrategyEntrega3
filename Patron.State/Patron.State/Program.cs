using Patron.State.Helper;

Reproductor reproductor = new Reproductor();

reproductor.Play();    // Inicia la reproducción
reproductor.Pause();   // Pausa la reproducción
reproductor.Play();    // Reanuda la reproducción
reproductor.Stop();    // Detiene la reproducción
